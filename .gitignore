__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Personal floor plans — do not publish (repo is public)
data/soni_residence/
data/cubicasa5k/
# Second drafting corpus, 122 scanned plans in four styles. Downloaded,
# not ours to redistribute, and the page states no licence.
data/cvc_fp/
# A sample of BRIDGE, real Indian plans. Bounding boxes only -- no
# structural ground truth -- so it is for looking at, not scoring.
data/bridge/

# Generated outputs. The glob rather than a list of names: two output
# directories were committed by accident because they were called
# output_other and output_test rather than output, and they carried 22 MB
# of rendered pages of the personal plans excluded above.
*.glb
*.obj
output/
output_*/

# Trained checkpoints (~98 MB each)
models/*.pt
output_unet/

# Superdesign working files (drafts are held server-side)
.superdesign/tmp/

# Subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/
